# golangci-lint configuration (v1 schema).
# Keeps the default linters and adds revive plus gofmt/goimports formatting.
run:
  timeout: 5m

linters:
  enable:
    - revive
    - gofmt
    - goimports

linters-settings:
  goimports:
    local-prefixes: github.com/akuaku-ai/akuaku
